{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build server with source map",
            "type": "shell",
            "command": "cd ${workspaceFolder}/packages/server && npm run build-debug"
        }
    ]
}
